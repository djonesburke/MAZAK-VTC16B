o<clamptool> sub

M65 P5 ; turn off tool unclamp solenoid
M64 P2 ; turn on tool clamp solenoid
G4 P.3 ;dwell
M65 P2 ; turn off tool clamp solenoid
M66 P5 L3 Q2 ; check the clamped tool sensor
o100 if [#5399 LT 0]
    (abort, Failed to clamp tool) ; abort if the sensor does not activate in 2 seconds
o100 endif

o<clamptool> endsub [1]

M2
